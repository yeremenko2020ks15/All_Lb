<%= form_with(model: klinik) do |form| %>
  <% if klinik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(klinik.errors.count, "error") %> prohibited this klinik from being saved:</h2>

      <ul>
        <% klinik.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :founding_date %>
    <%= form.text_field :founding_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
